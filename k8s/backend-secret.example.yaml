# TEMPLATE ONLY — do NOT commit real secrets.
#
# For local bootstrapping you can `kubectl apply -f` this file AFTER
# replacing the placeholder values with real, base64-encoded strings.
# For production, prefer creating the Secret via `kubectl create secret`
# (see k8s/README.md) or with AWS Secrets Manager + External Secrets.
#
# Encode values with:
#   echo -n 'mongodb+srv://user:pass@cluster/gullyscore' | base64

apiVersion: v1
kind: Secret
metadata:
  name: gullyscore-backend
  namespace: gullyscore
  labels:
    app.kubernetes.io/name: gullyscore-backend
type: Opaque
data:
  MONGO_URI: PFJFUExBQ0VfV0lUSF9CQVNFNjRfTU9OR09fVVJJPg==
  ADMIN_TOKEN: PFJFUExBQ0VfV0lUSF9CQVNFNjRfQURNSU5fVE9LRU4+
